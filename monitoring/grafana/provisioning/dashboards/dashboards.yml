apiVersion: 1
providers:
  - name: "clinical-agent"
    orgId: 1
    folder: "clinical-agent"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
